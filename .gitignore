# Raw Yelp dump (academic-use license — do not commit)
Yelp JSON/
*.json.gz

# Generated data artifacts — leading `/` anchors to repo root so the patterns
# don't accidentally hide `src/rrs/features/`, `src/rrs/labeling/`, etc.
/data/
/labels/
/features/
*.duckdb
*.duckdb.wal
*.parquet
reports/figures/

# Python
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
*.egg-info/

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/

# Local Claude Code state (settings.local.json is per-user; lock file is transient)
.claude/settings.local.json
.claude/scheduled_tasks.lock
